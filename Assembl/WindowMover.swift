import Foundation
import Cocoa


// Handles the calculation and moving of windows around the screen
//
class WindowMover {
    /**
     The primary init function for calculating and resizing all windows.
     */
    static func assemble() {
        print (NSApp.orderedWindows)
    }
}
